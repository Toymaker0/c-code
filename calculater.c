#include <stdio.h>
int main()
{
int x,y,z;
 printf("Enter the number x: " );
 scanf ("%d",&x);
 printf("Enter the number y :");
 scanf("%d",&y);
 z=x+y;
 printf("The sum is :%d",z);
return 0;
}